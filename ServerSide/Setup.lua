--Sets up remote events/other things
local event1 = Instance.new("RemoteEvent")
event1.Parent=game:GetService("ReplicatedStorage") -- // Again; use GetService, don't call the service directly.
event1.Name="event_121134"
